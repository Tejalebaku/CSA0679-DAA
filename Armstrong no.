#include<stdio.h>
int main()
{
	int n,r,a,sum=0;
	printf("enter the num = ");
	scanf("%d",&n);
	a=n;
	while(n!=0)
	{
		r=n%10;
		sum=sum+r*r*r;
		n=n/10;
	}
	if(sum==a)
	{
		printf("armstrong num");
	}
		else
		{
			printf("not armstrong num");
		}
		
	}
	
